import ThreeScene from '@/components/ThreeScene'
import { createLoadModel } from '@/examples/threejs/utils'

export const meta = {
  title: 'Ninja Animation',
  date: '2023-03-20',
}

export function getStaticProps() {
  return { props: { meta } }
}

<ThreeScene
  load={createLoadModel({
    modelUrl: '/assets/emeraldwalk/ninja.animated.glb',
    cameraPosition: [1, 2, 3.5],
  })}
/>
